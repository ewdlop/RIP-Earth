with(DifferentialGeometry):
with(Tensor):

# Define the coordinates
DGsetup([x, y], M):
DGsetup([x, y, f(x, y)], N):

# Define the surface z = x^2 + y^2
f := (x, y) -> x^2 + y^2:

# Define the frame
Frame := DGinfo(N, 'Frame'):

# Calculate the first fundamental form
FirstFundamentalForm := DGinfo(N, 'FirstFundamentalForm');

# Calculate the second fundamental form
SecondFundamentalForm := DGinfo(N, 'SecondFundamentalForm');

# Calculate the principal curvatures
PrincipalCurvatures := DGinfo(N, 'PrincipalCurvatures');

# Calculate the mean curvature
MeanCurvature := simplify(add(PrincipalCurvatures)/2);

# Calculate the Gaussian curvature (which is the scalar curvature for 2D surfaces)
GaussianCurvature := GaussianCurvature(N);

# Output the results
MeanCurvature;
GaussianCurvature;
